export interface LeetCodeRectangleOverlap {
    rectangleOverlap(rec1: number[], rec2: number[]): boolean;
}

export interface LeetCodeLargestPerimeterTriangle {
    largestPerimeterTriangle(nums: number[]): number;
    //rectangleOverlap(rec1: number[], rec2: number[]): boolean;
}